# Contributing

Keep changes small and test the app in both desktop and mobile viewport widths.

Before opening a pull request:

```sh
pnpm install
pnpm build
```

Do not add the full Katunog download mirror. This repository should stay limited
to the selected Gabbang bundle used by the simulator.
